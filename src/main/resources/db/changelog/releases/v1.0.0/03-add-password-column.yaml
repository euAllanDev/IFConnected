databaseChangeLog:
  - changeSet:
      id: add-password-column-users
      author: paulogx77
      changes:
        - addColumn:
            tableName: users
            columns:
              - column:
                  name: password
                  type: varchar(255)
                  constraints:
                    nullable: false